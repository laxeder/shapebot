export enum CMDAvaible {
  Cumunity = "community",
  Chanel = "chanel",
  Group = "group",
  Chat = "chat",
  Pv = "pv",
}

export enum CMDRun {
  Exec = "onExec",
  Reply = "onReply",
}

export enum CMDLevel {
  Max = 0,
  Config = 1,
  Text = 2,
  Common = 3,
  CommonSecundary = 4,
}
